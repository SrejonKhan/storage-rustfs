# RustFS — copy to .env and set values (or set in Coolify env vars).
# Required: RUSTFS_SECRET_KEY, RUSTFS_SERVER_DOMAINS (deploy fails if missing).

RUSTFS_ACCESS_KEY=rustfsadmin
RUSTFS_SECRET_KEY=ChangeMeStrongPassword123
RUSTFS_SERVER_DOMAINS=storage.example.com
RUSTFS_CONSOLE_ENABLE=true
